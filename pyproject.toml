[project]
name = "reranker-comparison"
version = "0.1.0"
description = "A comparison of different reranking models"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "cohere>=5.14.0",
  "einops>=0.8.1",
  "flagembedding>=1.3.4",
  "flash-attn>=2.7.4.post1",
  "python-dotenv>=1.0.1",
  "transformers>=4.49.0",
]
